<% provide(:title, "#{@category.name}がある学校リスト")  %>
<div class="container">
  <%= link_to :back, class:"btn-back" do %>
  <i class="fa fa-arrow-left" aria-hidden="true"></i> Back
  <% end %>
  <div class="row">
    <div class="col-md-12">
      <h1 class="title-school"><%= @category.name %>がある学校のレビューリスト</h1>
      <%= render "layouts/review_index", reviews: @reviews %>
    </div>
  </div>
  <%= paginate @reviews %>
</div>
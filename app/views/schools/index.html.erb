<% provide(:title, "語学学校リスト")  %>  
<%= render "slide" %>
<%= render "search_form" %>

<div class="container">  
  <div class="row">
    <% @schools.each do |school| %>
    <div class=" col-lg-4">
    <%= link_to school_path(id: school.id) do %>
      <div class="card mt-5 shadow-sm" style="width: 20rem; margin: auto;">
        <% if school.image_url_a.present? %>
          <%= image_tag "#{school.image_url_a}", class: "card-img-top"%>
        <% else %>
          <%= image_tag "#{school.image_url_b}", class: "card-img-top"%>
        <% end %>
        <div class="card-body webkit-center">
          <div class="title-school-index"><%= school.name %></div>
          <p class="card-text"><%= render "genre", school: school %></p>
          <p class="school-card-text">
            <i class="fa fa-comment" aria-hidden="true"></i>
            レビュー件数 <%= school.reviews.count %> 件</p>
        </div>
      </div>
    </div>
    <% end %>
    <% end %>
  </div>
  <div class='kaminari_paginate'>
    <%= paginate @schools %>
  </div>